#pragma once

#include <string>
#include <vector>
#include <ncurses.h>

//#include "world.hpp"

class CMenu
{  
public:
    //CMenu();
    //~CMenu();

    //CWorld * loadLevel();
    //int pickLevel();
    void render(WINDOW * _w);
    //void newGame();
};

